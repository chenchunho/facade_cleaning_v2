# rail —— 只驅動上滑台（DM2J:14），刻意避開 zdt_pusher。
#
# 🔴 存在的理由：smoke.txt 走到的預期差異第 7 條（分派器範圍分岔）在基準上是
#    「指令根本不能執行」，它**遮蔽了所有下游的預期差異**。這份腳本繞過它，
#    去走上滑台那條路 —— 那裡有三條預期差異：
#      ① 上滑台 cm↔pulse 換算 7.731（基準是 1.0 → 同一個 cm 指令送出的脈衝差 7.7 倍）
#      ② ARM_SWEEP_RPM 1000→250
#      ⑧ DM2J 16 個 void 改回傳 bool（寫入失敗終於看得見）
ping
status
arm_sweep
status
