# 程式架構重構計畫

> **狀態：** 🟡 進行中（2026-08-29 建立）
> **規範權威：** 本檔。分層的最終樣貌一旦定案，會併進 `CLAUDE.md` `## Architecture`；
> 本檔只在「進行中」存在，完成或作廢就移進 `.claude/archive/`。
> **待辦不寫在這裡** —— 唯一待辦總表在 `work_log.md` 最上方。

---

## 0. 這份計畫要回答的問題

**把程式整理成分層架構，而且證明整理完之後功能效果沒有變。**

📌 參考文件 `architecture.md`（多軸馬達控制系統的通用架構描述）**只是參考**，
不是要照抄的規格 —— 它假設的東西有一部分在這台機器上不成立（見 §2）。

---

## 1. 成功判準（🔴 先定義成功，再開始動）

> **「跟 `main` 最後一版功能效果一樣」才算整理成功。** —— per user 2026-08-29

但**基準不能是 `main` 本身**。本分支相對 `main` 有 **9 條刻意的行為改變**，
其中好幾條修的是 `main` 上此刻正在壞的東西：

- `main` 的上滑台每次掃動下 **131 cm** 指令而滑台只有 50 cm ——**每次都撞到底**
- `main` 的吸盤左右歸屬錯誤 → **交替步伐不可用**
- `main` 的推桿換算差 5%

所以可用的基準只有一個：

```
等價基準 ＝ main-final 的功能  ＋  runbook.md §A2 塊三那 9 條預期差異
```

那份清單已經存在（2026-08-29 寫的上機檢查表），**它從一份上機檢查表升格為重構的等價規格**。

| 比對結果 | 判定 |
|---|---|
| 差異 ⊆ 那 9 條 | ✅ 整理成功 |
| 出現清單外的差異 | 🔴 搬壞了 —— 而且能指出是哪一階段搬壞的 |

---

## 2. 為什麼不照抄 `architecture.md`：「軸」這個抽象在這台機器上不成立

參考架構的 `IAxis`（`move_abs(pos, speed)` / `read_state()`）假設 **一個裝置 = 一個軸**。
實際盤點 14 支 driver：

| 類別 | 裝置 | 數量 |
|---|---|---|
| **真正的位置軸** | ZDT（吸盤推桿）、DM2J（上滑台） | **2** |
| **VFD：只有速度輸出、無位置回授** | SE3 ×2、MH300、CLV900 | 4 |
| **感測器（不是軸）** | SD76 計米 ×3、DSZL 張力 ×2、JC100 真空 ×4、DY500 重量 ×2、XKC 水位、WT901 IMU | 13 |
| **開關型致動器（不是軸）** | PQW 繼電器 ×2、QX-DO24 PWM、ZS-DIO | 4 |

🔴 **吊機的一條繩 = 三個裝置、三條匯流排**：
SE3 變頻器（USR_A/B，速度輸出）＋ SD76 計米器（USR_M，位置回授）＋ DSZL-107（X518 獨佔，張力）。
而且**是刻意拆開的**（`Crane_control_PI/main.cpp` 開頭寫著理由：單一 bus 掛掉時語意單純）。

→ 強行套 `IAxis`，這個閉環會沒有家。它現在就住在 `Crane_control_PI/main.cpp` 裡，跟協定攪在一起。

---

## 3. 目標分層：4 → 6 層 ＋ 1 個橫切

```
介面層 GUI              web_backend（Node，已是獨立行程）
      ↓ 文字協定
🆕 指令層 Command       84 個 cmd_* 的家：解析、FAST/SLOW 路由、參數驗證
      ↓ 流程命令
任務層 Task             狀態機、步態編排、sequence
      ↓ 工程單位（cm / kg / 度）
🆕 機構層 Mechanism     虛擬軸：速度輸出 + 位置回授 + 力回授 → 組成一個軸
      ↓ 裝置命令
驅動層 Driver           協定、暫存器、位元組序          user_lib/
      ↓ bytes
傳輸層 Transport        ✅ 已完成（2026-08-27）          transport/

橫切：安全層 Safety     監看 + 否決，**不在呼叫路徑上**
```

### 3.1 🆕 機構層 Mechanism（新增，三個理由每一個都踩過）

1. **虛擬軸**：吊機繩 = SE3 + SD76 + DSZL 的組合。閉環在這層，不在 driver 也不在 task
2. **機構標定的家**：`7.731 cm/圈` 是**皮帶軸**的性質，不是驅動器的。
   放進 `device_profile` 的話，換一顆同型馬達就會把標定重置 ——
   而它錯了的症狀是每次掃動下 131 cm 指令、滑台只有 50 cm，**四個月沒有人被告知**
3. **行程守衛**：`travel <= 48 cm` 由標定推導，屬於機構不屬於協定

### 3.2 🆕 指令層 Command（新增）

`WASH_ROBOT.cpp` 裡 **84 個 `cmd_*` vs 13 個 `do_*`** —— 六比一，介面協定壓過流程邏輯。

存在的直接理由：2026-08-27 把吸盤編號 1-4 改成 5-8 之後，
`zdt_pusher` / `zdt_disable` / `zdt_enable` **變成不可能成功** ——
分派器收 1-4、應用層收 5-8，**兩個範圍沒有交集**。
範圍驗證與被驗證的常數分居兩處就會分岔；指令層明確化之後，這件事變成「同一層吃同一個常數」。

🔴 **FAST / SLOW 雙路徑必須保留**：`stop` / `estop` / `status` / `pause` 走同步路徑。
理由是修過的 bug —— 長時間運動指令佔住收包執行緒時 `stop` 送不進去 ＝ **GUI 死鎖**。
**急停排在一個 15 秒的 move 後面，等於沒有急停。**

### 3.3 安全層 Safety（橫切，刻意不在呼叫路徑上）

放在呼叫路徑上的閘門遲早會被繞過 —— 2026-08-28 就有實例：合併帶進的
`sendAndReceiveQuiet()` 繞過同期加的 `MSG_NOSIGNAL` 防線，**git 一句話都沒說**。

→ 安全層應該是**獨立監看執行緒 + 否決權**（像現在的 `hold_loop()`），不是穿透式閘門。

### 3.4 `cleaning_arm` 不拉進來

3,041 行、**不使用 `user_lib`**、自建 socket 層、走 CAN，透過 TCP `:9527` 對外。
它**已經是一個服務邊界**，而且是全專案最乾淨的隔離。
參考架構沒有「跨行程服務」這個概念，但它在這裡是既成事實且運作良好 ——
**明確寫成架構的一部分，不是待重構的債。**

---

## 4. 現況數字（2026-08-29 實測，重構的起點）

| 項目 | 數字 |
|---|---|
| `app/WASH_ROBOT.cpp` | 起點 **13,435 行**（全 repo 44%）→ ✅ **階段 1 後 9,442 行** |
| ├ `cmd_*` / `do_*` | **84 / 13** |
| └ `#if 0` 死碼 | 18 塊、**3,993 行** → ✅ **已刪（2026-08-29，經證明是 no-op）** |
| `app/WASH_ROBOT.h` | 2,646 行，**228 個 `static constexpr`** |
| `Crane_control_PI/main.cpp` | 4,514 行，**應用層從未抽出** |
| `Linux_test/main.cpp` | 5,945 行 |
| `cleaning_arm/main_api.cpp` | 3,041 行（獨立服務） |
| `user_lib/` 14 支 driver | 6,022 行，**零共同抽象**（無 virtual、無基底類別） |
| `transport/` | 1,124 行 ✅ 已是目標架構的樣子 |

---

## 4.1 🆕 `Crane_control_PI` 拆解分析（2026-08-29，階段 3 的前置）

4,514 行，應用層從未抽出。實測結構：

| 成分 | 數量 | 歸屬 |
|---|---|---|
| `cmd_*` 函式 | **37** | → 🆕 指令層（本體是 84，兩支主程式同樣的問題） |
| 背景執行緒 | **6**（`main_loop`／`hold_loop`／`meter_loop`／`balance_loop`／`watchdog_loop`／`vfd_keepalive_loop`） | → 任務層的常駐部分；`hold_loop` 是**現成的安全層樣板**（監看 + 否決） |
| driver 實例 | 12（`vfd_left/right`、`meter_left/right/middle`、`dsz_left/right`、`inverter`、`pqw_water`、5 個 `cli_*`） | → 驅動層（已在 `user_lib/`） |

### 🔴 最強的證據：虛擬軸其實已經存在，只是被寫成命名慣例

以 `_left`／`_right` 成對出現的識別字（**約 340 處**）：

| 識別字 | 成對次數 |
|---|---|
| `vfd_*` | 86 |
| `g_length_*` | 48 |
| `meter_*` | 29 |
| `dsz_*` | 28 |
| `hold_up_*` / `hold_down_*` | 28 / 26 |
| `g_dev_vfd_*` / `g_dev_meter_*` / `g_dev_dsz_*` | 24 / 26 / 24 |

**一條繩 = `vfd`（速度輸出）＋ `meter`（位置回授）＋ `dsz`（張力）**，
左繩 10+4+5 次直接呼叫、右繩 11+4+10 次。
而且已經有 `resolve_meter_side()` 這種「把 side 映射到裝置」的臨時 helper。

📌 **也就是說：機構層在概念上早就存在，只是拼寫成 `_left`/`_right` 後綴而不是一個型別。**

🔴 **不做這層的代價是可量測的**：每一個新功能都得記得「左邊做一次、右邊做一次」。
2026-08-28 抓到的「吸盤左右歸屬 RF={5,6}/LF={7,8} 錯了四個月、
導致每一個分側判準實際上都在看『一邊各一顆』＝等於沒有保護」，
就是同一個 class 的缺陷在本體那邊的實例。

→ **階段 3 的具體目標**：抽出 `RopeAxis`，兩個實例（left / right）。
把 340 處成對識別字收斂成 `rope_[side].xxx`，並把閉環（VFD 出力 → SD76 讀位置 →
張力守衛）搬進這個型別。**「側」變成參數而不是名字的一部分。**

---

## 5. 🔴 等價性怎麼證明：黃金軌跡（golden trace）

**這支程式對外的契約 ＝「匯流排上的位元組」＋「TCP 回的文字」。**
兩者都一樣，重構就是正確的 —— 定義上如此，不需要靠人判斷。

→ 同一份指令腳本，分別餵給 `main-final` 與重構版，**兩者對假從站送出的位元組序列必須逐位元相同**。

### 已經有的（90%）

| 元件 | 現況 |
|---|---|
| **bytes 層 trace** | `LOG_HEX` 已在 15 支 driver（ZDT 就有 35 處），格式統一 `[時間] [DBG] [DEV:ID] TX xx xx` |
| **假從站** | `Linux_test/fake_slaves/` 已進版控：RTU-over-TCP + MBAP 兩種，可故意送壞幀 |
| **指令入口可腳本化** | 兩支主程式本來就走 TCP 文字協定（`:5001` / `:5002`），指令用管線灌 |

### 缺口（階段 0 要補的）

1. **端點注入**：5 個寫死的 IP 常數 + 17 個 `init()` 呼叫點
2. **假匯流排**：現有假從站一次只服務一個 slave，要能一台服務整條 bus
3. **正規化 + diff**：時間戳、連線順序、非決定性欄位
4. 🔴 **負控制**：先證明工具會失敗

### 🔴 實作時撞到的四個「儀器缺陷」（2026-08-30，每一個都會偽裝成行為差異）

| # | 缺陷 | 在 diff 上長什麼樣 |
|---|---|---|
| 1 | 基準側用舊版非原子 `LOG_HEX`，行被打亂 | 某裝置「只有 RX 沒有 TX」 |
| 2 | `normalize.py` 用 `^` 錨定，driver log 被應用層 `cerr` 接在前面時**靜默丟棄** | 「這一側少一次交易」 |
| 3 | 收尾靠 kill，而關閉序列對連不上的吊機無界重試 | 兩側停在不同時間點，尾端整段不同 |
| 4 | 假從站回泛用亂值 → 運動永遠等不到「到位」 | 只測到逾時分支，且一輪要跑好幾分鐘 |

📌 **通則：量測儀器必須兩側一致，而且收尾必須是確定性的。**
   拿一份被打亂的量測去比一份乾淨的，比出來的差異是儀器造成的 ——
   而它跟真正的行為差異在 diff 上長得一模一樣。

### ⚠️ 已知的結構性限制：被走到的那條預期差異會**遮蔽**其餘的

`smoke.txt` 走到的是第 7 條（分派器範圍分岔），而它在基準上的表現是
**「指令根本不能執行」** —— 基準從來沒走到下游，自然比不出換算（7.731）、
左右歸屬、回覆驗證那些差別。
→ **9 條預期差異目前只驗到 1 條。** 要驗其餘 8 條，需要一個「該指令能執行」的
中間基準（例如 `0d5f6bc` 之後、`8c59eac` 之前的某個 commit）。

### 🔴 為什麼「負控制」不能省

2026-08-29 才踩過：`test_qx_do24` 的斷言寫反（`if (init(...)) FATAL`，而 `QX_DO24::init()`
是 `true`=成功），**一個斷言都沒跑到卻回報通過**，那支測試從寫出來到當天從沒真正跑起來過。

→ **工具上線前必須先故意改壞一個常數，確認 diff 會紅。** 沒有紅過的綠燈不算綠燈。

---

## 5.5 🆕 驗證有兩級，先用便宜的那級

2026-08-29 做階段 1 時發現的：**「trace diff = 0」不是最強的判準，也不是最便宜的。**

| 工具 | 證明什麼 | 成本 | 什麼時候用 |
|---|---|---|---|
| **`harness/prove_noop.sh`** | **整個編譯單元的預處理輸出逐位元相同** | 幾秒，不需要 g++、不需要跑任何東西 | 純刪死碼、純搬動、只改註解 |
| `harness/compare.sh` | 跑到的路徑上，位元組與回覆相同 | 要建置 + 執行 | 真的改變了結構（抽函式、換介面） |

🔴 **`prove_noop` 更強的地方**：`compare.sh` 只涵蓋指令腳本跑到的路徑
（覆蓋率是它最弱的一環）；`prove_noop` 涵蓋**每一行**。
🔴 **它做不到的地方**：只要語意真的變了它一定紅，所以抽函式那種改動用不上它。

**原則：能用 `prove_noop` 證的階段，就不要動 `compare.sh`。**

---

## 6. 階段與完成判準

| 階段 | 內容 | 完成判準 | 需要機器？ |
|---|---|---|---|
| **−1** | 🔴 **HEAD 上機跑一次**，對照 `runbook.md` §A2 | 差異 == 那 9 條 | **是** |
| **0** | tag `main-final`；端點注入；假匯流排；指令腳本 + 正規化 + diff | ⚠️ **負控制先紅** | 否 |
| **1** | 刪 18 塊 `#if 0` | ✅ **完成**：`prove_noop` 逐位元相同（比原訂的 trace diff 更強） | 否 |
| **2** | 抽**指令層**（84 個 `cmd_*`） | trace diff = 0 | 否 |
| **3** | **機構層／虛擬軸**（吊機繩） | trace diff = 0 | 否 |
| **4** | config 外部化（機構 profile 與 device profile **分兩份**） | trace diff = 0 | 否 |
| **5** | 拆 `WASH_ROBOT.cpp` 成 sequence | trace diff = 0 | 否 |

### 排序的理由

- **階段 −1 是地基**：本分支的 9 條行為改變**一條都沒有實機證據**（2026-08-29 只做到
  `cl /Zs` 語法層）。在未驗證的基礎上重構，日後出現差異時**分不出是重構搬壞的、
  還是那 9 條本來就有問題**。這正是放棄「分兩段上機」時付出的代價 —— 歸因能力沒了。
  🔴 **但機器時間未定 → 階段 0 先做，−1 排到機器空出來時補。**
- **階段 1 刻意選「刪死碼」打頭陣**：預處理器本來就丟掉了，所以它是**唯一一個
  trace 必須完全不變的動作** —— 它同時是**驗證工具自己的第一次真實檢驗**。
  刪死碼若跑出差異，那是工具壞了不是程式壞了。
- **原本把吊機排第一是為了避開另一位工程師**。`main` 於 2026-08-29 凍結後
  這個理由消失，排序改由「什麼能解鎖驗證」決定。

---

## 7. 兩個已知陷阱（動手前先讀）

### 7.1 config 外部化會沖掉「理由」

那 228 個常數裡有些帶著**承重的沿革註解**：

- `CH6`（破真空）與 `CH14`（水泵）**絕不可同號** —— 同號＝清洗時開水泵等於開破真空閥，
  四顆吸盤同時失效、**機器在貼牆狀態下脫落**
- `CUP_PULSE_PER_CM = 3000` 的四條獨立證據
- `ARM_RAIL_LEAD_CM_PER_REV = 7.731` 的最小平方殘差

YAML 放不下 20 行理由。

→ **標定值**外部化並帶 provenance 欄位（來源／量測日期／方法／殘差）；
**互鎖規則**留在程式碼裡當斷言，**不要外部化成一個可以被人改錯的數字**。

### 7.1.1 🆕 225 個常數的分類盤點（2026-08-29，階段 4 的前置）

| 類別 | 數量 | 外部化政策 |
|---|---|---|
| **安全互鎖** | **12** | 🔴 **不外部化**。留在程式碼裡當斷言 —— 一個可以被人改錯的數字不是保護 |
| **機構標定** | **6** | ✅ 外部化到 `axis_profile`，**必須帶 provenance**（來源／量測日期／方法／殘差） |
| **設備協定** | **25** | ✅ 外部化到 `device_profile`（slave、通道、端點） |
| **流程參數** | 182 | ✅ 可外部化 |

**12 個安全互鎖**：`IMU_EMERGENCY_DEG` 45°／三個 `*_WALL_MM` 400／`VACUUM_EARLY_STOP_KPA` −45／
`DISABLE_PHASE_CURRENT_LIMIT_MA` 1200／`DISABLE_POS_ERROR_LIMIT_DEG` 5.0／
`ROPE_WEIGHT_LIMIT_KG_PER_SENSOR_{ATTACHED,HANGING}` 40／80／`BAL_CAL_UP_STOP_TOTAL_KG` 100／
`BAL_CAL_INNER_STALE_LIMIT` 60／`BAL_CAL_TENSION_MIN_KG` 10／`ARM_ROPE_PROTECT_WALL_MM` 400。

**盤點順帶抓到三件事：**

🔴 **① 三個 `*_WALL_MM` 都是 400，而且沒有任何機制保證它們一致。**
`DM2J_ARM_STEP_SWEEP_WALL_MM`（`:901`）／`ARM_CLEAN_WALL_MM`（`:986`）／
`ARM_ROPE_PROTECT_WALL_MM`（`:1587`），全檔 **0 個 `static_assert`**。
**而且歷史上已經分岔過**：`:900` 的註解到現在還寫著
`— separate from ARM_CLEAN_WALL_MM(330)`，但那個常數早就是 400 了。
`:1587` 的註解寫「跟 `ARM_CLEAN_WALL_MM` 統一」—— 那是**人工同步的意圖，不是機制**。
⚠️ **原本建議「讓三邊吃同一個常數」是錯的（2026-08-29 同日更正）**：讀完沿革，
`DM2J_ARM_STEP_SWEEP_WALL_MM` 在 2026-07-24 建立時**刻意 separate**（當時 330 vs 360），
三個歷史上真的分開調過 —— **合併會摧毀一個真實存在的能力**。
但最近兩輪（07-27、08-28）三個是一起改的，實務上已是同一個旋鈕。
→ ✅ **已改為「共同起點 + 可個別覆寫」**（`ARM_WALL_MM_DEFAULT`）：改預設值三個一起動，
要分開就把該處換成字面值並寫明理由。

🟡 **② `DISABLE_POS_ERROR_LIMIT_DEG = 5.0` 宣告了但全專案沒有任何一處讀它**
（`.h` 2 次＝宣告與註解，`.cpp` 0 次）。註解自己標著 `(currently unused)` 算誠實，
但它擺在安全常數群裡，**會讓人以為「有這個保護」**。要嘛接上，要嘛刪掉。

🔴 **③ 有 21 個常數被「同名的 `#define`」遮蔽 —— 同一個識別字在檔案前後半是不同的東西**

```
app/WASH_ROBOT.cpp:1308  #define PUSHER_EXTEND_FEET_PULSE_LOWER  (settings_....load())
                  :1310  #define PUSHER_EXTEND_BODY_PULSE_SHORT  (settings_....load())
                  :1314  #define ROPE_WEIGHT_LIMIT_KG_PER_SENSOR_ATTACHED (settings_....load())
                  :1323  #define DISABLE_PHASE_CURRENT_LIMIT_MA  (settings_....load())
```

其中**三個是安全互鎖**（撞障礙物電流保險、繩重上限 ×2）。
⚠️ **原記「4 個」是錯的（2026-08-29 同日更正）**：grep 要求名稱與 `(settings_` 之間恰好一個空格，而那些是**對齊排版的多個空格**，只有 4 個長名稱剛好擠掉對齊。
從 `.h` 讀到 `static constexpr double ... = 40.0;` 會以為那就是生效的值；
實際上 `#define` 之後同名的東西是**執行期可調的 settings**。

✅ **今天沒有 bug**：`#define` 之前的兩處用法正好都合理需要預設值
（`:75` 用常數初始化 settings、`:1198` 印「現值:預設值」）。**這個慣用法是刻意的。**

🔴 **但它是埋著的地雷**：任何人在 `:1308` 之前新增引用這些名字的程式碼，
**會靜默拿到編譯期預設值而不是操作者調過的現值** —— 而安全門檻讀到預設值而非現值，
是錯的那個方向。編譯器不會警告，執行期也沒有訊號。
📌 **階段 4 會正面撞上這個**：把「常數」搬進 config 時，這 4 個根本不是常數。
建議在階段 4 之前先把這個慣用法換成明確的 `settings_.xxx.load()` 呼叫，
讓「預設值」與「現值」在原始碼上長得不一樣。

---

### 7.2 「合併乾淨 ≠ 語意接得上」在重構時同樣成立

搬動程式碼時 git 只看文字。2026-08-28 的實例：對方新增的函式帶著 `send(..., 0)` 進來，
繞過我方同期加的 `MSG_NOSIGNAL`，兩邊沒碰到同幾行 —— **git 一句話都不會說**。

→ 每個階段結束時，除了 trace diff，還要問一句：
**「這次搬動有沒有讓某條防線變成繞得過去的？」**

---

## 8. 進度

| 階段 | 狀態 |
|---|---|
| −1 | ⏸ **等機器** |
| 0 | ✅ **完成（2026-08-30）**：`compare.sh main-final-harness HEAD` 端到端跑通。
`readonly.txt` 兩個判準完全相同；`smoke.txt` 的差異 **100% 歸因於預期差異第 7 條**，
**無任何清單外差異**。基準分支 `main-final-harness` = `main-final` + 端點注入 + log 原子性 |
| 1 | ✅ **完成（2026-08-29）**：刪 18 塊 `#if 0`／3,993 行，`WASH_ROBOT.cpp` 13,435 → 9,442 行。**經 `prove_noop` 證明是 no-op**（非空行 110,433 = 110,433），負控制已驗 |
| 2~5 | ⚪ 未開始 |
