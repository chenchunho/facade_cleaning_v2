# `cycle_test.py` 完整週期測試（含風扇 + 手臂）：計畫

> 建立 2026-09-09（per user 選 A）。**尚未執行，等拍板。**
> 前置事實全部於本日實查。機器現況：頂端、`L=-225 / R=-225`、`state=idle`、走 **WiFi**。

---

## 這輪跟今天已跑的兩輪差在哪

已跑：`mission_run.py` ×2 輪 = **純吊機行走**，沒有風扇、推桿、真空、手臂。
本輪：`cycle_test.py` = **完整週期**，四個子系統全部加進來。

⇒ 🔴 **一次加入四個新變因**。09-02 兩次中止都不是姿態問題，是**裝置通訊**
（`extend_raw TIMEOUT`、`pwm_freq_write_failed`）。所以下面的煙霧測試不是形式。

---

## 🔴 需要你確認的一件事：`init` 會讓手臂動

`cmd_init` 的實際副作用（逐行查證）：

| 動作 | 會不會動機構 |
|---|---|
| PQW 繼電器 → **幫浦 ON**、閥/水 OFF | 幫浦啟動 |
| ZDT 驅動器 enable + 清 stall flag | 不動 |
| **推桿** | ✅ **不動**（v2 起 `init` 不伸任何推桿，四顆維持 0） |
| 🔴 **damiao 手臂 INIT** | **會動手臂** —— 註解寫明是「在一個受控的時機」做校正 |

⇒ **這對應待辦表那條「手臂 `motor_api` 未啟動，等 per user 確認周邊淨空」。**
**送 `init` 之前需要你確認手臂周邊淨空。**

---

## Phase 0 — 前置（四項，全部會擋住開跑）

| # | 項目 | 現況 | 處置 |
|---|---|---|---|
| 0.1 | **`motor_api` 未啟動** | 🔴 本體上完全沒有 motor 行程；binary 在 `~/bringup/motor_api`（09-04，444KB） | 起它。手臂端點是 **`127.0.0.1:9527`**（跑在本體上） |
| 0.2 | **真空幫浦 OFF** | 🔴 程式重啟後所有繼電器都是 OFF | **送 `init`**（腳本自己不送，它只會擋下來並告訴你要送）。⚠️ 這一步會動手臂，見上 |
| 0.3 | 🔴 **`TOP` 是寫死的常數 231** | 實測跨距 **~224~227**，且**會漂** | 見下方「TOP 怎麼處理」 |
| 0.4 | 起點在頂端 | ✅ 現在 `L=-225`（高度 225） | 不用動 |

### 🔴 TOP 怎麼處理 —— 建議照今天 `mission_run` 的做法

`cycle_test.py:136` 是 `TOP, BOTTOM = 231, 0`，**寫死、不是參數**（與 `mission_run.py` 不同）。
而 `abs(L0 - TOP) > TOL(5)` ⇒ 現在 `abs(225-231)=6` ⇒ **會拒絕啟動**。

**建議**：改成環境變數覆蓋、**預設維持 231**，跟今天 `FCV_WROBOT_HOST` 同一個形狀：
```
FCV_TOP_CM=225 python3 cycle_test.py 10 5 40
```
🔴 **刻意不直接改掉 231**：那個值帶著 09-03 的量測脈絡，改掉會讓下次有人以為 231 從來不存在。
📌 而且**跨距每輪會漂**（今日量到 223→226→227→225），寫死任何一個數字都會再過期一次。

---

## Phase 1 — 煙霧測試：**先跑 1 週期**

```
FCV_TOP_CM=<開跑前實測> python3 cycle_test.py 1 5 40
```

一個週期 = 下降 5 步 × 40cm，每步做完整清潔動作，到底再拉回頂端。

### 🚦 Gate 1（缺一不可）
| 檢查 | 通過樣子 |
|---|---|
| 真空 | 開跑印 `真空幫浦 A 組（chN）ON ✅` |
| 手臂 | `DEPLOY_F` 回 `OK`（四種回覆三種行為，`obstacle` 會中止） |
| 風扇 | ①5% / ⑤7% 切換無 `pwm_freq_write_failed` |
| 推桿 | `extend_raw` 無 TIMEOUT |
| 姿態 | 無 roll/diff 中止 |

🔴 **任何一項失敗都停下來看，不自動重跑** —— 09-02 的兩次中止都是裝置通訊，重跑只會再撞一次。

---

## Phase 2 — 10 週期

```
FCV_TOP_CM=<實測> python3 cycle_test.py 10 5 40
```

### ⏱ 時間：**我沒有可靠的基線，估 ≥40 分鐘**
腳本自述的**增量**：③b 清潔動作每步 +14 秒（10 週期 50 步 ≈ **+12 分鐘**）、
`DEPLOY_F` 比 `DEPLOY` 再慢 7 秒/步（**+6 分鐘**）。
**但我沒有「不含這兩者」的基線耗時數字**，所以總時間是下限不是估計值。

### 對照基準（都在 WiFi 上、都跑滿 10/10）
| 輪次 | 左右差 |
|---|---|
| `cycle10_0902`（**無**真空） | 中位 3 / p90 5 / max 6、≥6cm 3% |
| `0902d`（**有**真空） | 中位 3 / p90 **4** / max **5**、≥6cm **0%** |

📌 09-03 已結案：**有真空不但沒惡化、還略好** ——「吸附/脫離是最大嫌疑變因」那個假說已被推翻。

---

## 🔴 已知會咬人的四件（都不是姿態問題）

| 風險 | 依據 | 這輪特別相關嗎 |
|---|---|---|
| **風扇干擾 `.22` 匯流排** | 09-02 兩份 log 一致：風扇開啟期間 `.22`（JC100×4、QX:9）錯誤密度 **≈15×**。病因已定性為電氣雜訊，機制未分辨 | 🔴 **是** —— per user 這輪就是要看風扇 |
| **QX-DO24 間歇寫入失敗** | `0902c` 被 `pwm_freq_write_failed_no_reply_timeout` 當場中止 | 🔴 是（風扇就掛在 QX-DO24） |
| **推桿 `extend_raw` TIMEOUT** | `0902b` 中止原因 | 是 |
| **JC-100 通訊錯誤** | 09-08 仍在，每 3~6 分鐘一筆 `TIMEOUT`（`_pt` 0→5ms 待評估） | 是 |

📌 **中止原因要分「機構／姿態」與「裝置通訊」兩類看**（09-03 立下的判準）。

---

## ⚠️ 三個要記住的邊界

1. **行程 200cm vs 跨距 ~225cm ⇒ 餘裕約 25cm。** 09-03 是 231、餘裕 31cm。
2. **走 WiFi，不是隧道** —— 這輪不算有線鏈路驗收。
3. **姿態基準已經偏了**：今日兩輪之後靜止 `raw_x` 由 −0.27° 累積到 **+3.72°**、張力差 19→29.5kg
   （per user：**捲筒本來就是這樣，計米器+IMU 修正就是為它而存在**）。
   ⇒ 🔴 **本輪的姿態數據不可與 09-02/09-03 那兩輪直接比對** —— 起始條件不同。
   要可比就得先讓機器回到接近水平的狀態，**那需要 per user 判斷怎麼做**。

---

## 我需要你拍板的

1. 🔴 **手臂周邊淨空了嗎**（`init` 會動手臂）
2. `TOP` 用環境變數覆蓋、預設不動 —— 可以嗎
3. 先跑 1 週期再跑 10 —— 可以嗎
4. ⚠️ 姿態已偏 3.72°，**要不要先處理再跑**（會影響數據可比性）
